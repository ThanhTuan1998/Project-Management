const NS_RESIDENT = "myResident";

const SET_LOADING = `${NS_RESIDENT}/setLoadingResident`;

const GET_ALL_RESIDENT = `${NS_RESIDENT}/getAllResident`;

const SAVE_ALL_RESIDENT = `${NS_RESIDENT}/saveAllResident`;

const CREATE_ACCOUNT_RESIDENT = `${NS_RESIDENT}/createAccountResident`;
const DELETE_ACCOUNT_RESIDENT = `${NS_RESIDENT}/deleteAccountResident`;
const GET_ID_USERS = `${NS_RESIDENT}/getIdUsers`;
const UPDATE_USERS = `${NS_RESIDENT}/updateUsers`;
export {
  SET_LOADING,
  GET_ALL_RESIDENT,
  SAVE_ALL_RESIDENT,
  CREATE_ACCOUNT_RESIDENT,
  DELETE_ACCOUNT_RESIDENT,
  GET_ID_USERS,
  UPDATE_USERS,
};
